{
  "name": "dl-lerna-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "push": "git add . && git commit -m 'commit script' && git push origin master"
  },
  "devDependencies": {
    "lerna": "^6.5.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
